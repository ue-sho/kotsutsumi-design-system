{
  "name": "kotsutsumi-design-system",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo run dev --cache-dir=.turbo",
    "build": "turbo run build --cache-dir=.turbo",
    "lint": "turbo run lint --cache-dir=.turbo",
    "lint-fix": "turbo run lint-fix --cache-dir=.turbo",
    "format": "turbo run format --cache-dir=.turbo",
    "format:check": "turbo run format:check --cache-dir=.turbo",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "build-storybook": "turbo run build-storybook --cache-dir=.turbo",
    "storybook": "pnpm --filter @kotsutsumi/ui storybook",
    "tokens": "pnpm --filter @kotsutsumi/tokens",
    "ui": "pnpm --filter @kotsutsumi/ui",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@changesets/cli": "2.31.1",
    "@commitlint/cli": "21.2.1",
    "@commitlint/config-conventional": "21.2.0",
    "lefthook": "2.1.10",
    "oxfmt": "0.60.0",
    "oxlint": "1.71.0",
    "turbo": "2.10.6",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22"
  }
}
