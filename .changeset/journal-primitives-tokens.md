---
'@uesho/kotsutsumi-tokens': minor
---

Add the `text.muted` and `border.subtle` semantic colors plus the `shadow` scale used by surface elevation.

Add an `overlay.black-6` primitive so `shadow.subtle` can reference a primitive color token.
