---
'@uesho/kotsutsumi-ui': minor
---

Add the journal UI primitives `Text`, `Heading`, `Card`, `Inline`, `Divider`, and `EmptyState`, all sharing the `primary | secondary | muted` tone API.

Refine `Prose` selector specificity so composed primitives can keep their own `size` and `tone` styles.
